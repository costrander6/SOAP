{
    "scanner": "actionlint",
    "findings": [
        {{- $first := true}}
        {{- range $ := .}}{{if $first}}{{$first = false}}{{else}},{{end}}
        {
            "title": "{{$.Kind}}",
            "description": {{json $.Message}},
            "file": "{{$.Filepath}}",
            "lineStart": {{$.Line}},
            "lineEnd": {{$.Line}}
        }{{end}}
    ]
}